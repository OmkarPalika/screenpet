## What this changes, and why

<!-- What is in the diff is visible. Why it needed to change is not. -->

Fixes #

## Checks

- [ ] `npm test` passes
- [ ] `npm run verify:ui` passes, or this does not touch the interface
- [ ] There is a test that fails without this change — and I watched it fail
- [ ] Documentation updated if this changed behaviour, a stored file, a
      dependency, or a feature count

## Does this send anything anywhere?

- [ ] No. Nothing new leaves the machine.
- [ ] Yes — and it is behind a switch that is off by default, with a row in
      `PRIVACY.md` naming what is sent and to whom, and a line in
      `CHANGELOG.md`.

<!--
Opening this pull request means you agree to the licence terms in
CONTRIBUTING.md, including the grant that lets the project be licensed
commercially. If that is a problem, say so here rather than closing the PR.
-->
